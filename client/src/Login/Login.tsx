import React, { PropsWithChildren } from "react";

export const Login: React.FC<PropsWithChildren> = () => {
  return (
    <div className="flex items-center justify-center w-full h-full">
      <button>login</button>
    </div>
  );
};
